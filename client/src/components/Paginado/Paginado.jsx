/* import React from "react";

export default function Paginado({ page, allCountries }) {
  const sigPage = () => {
    if (allCountries.length > page + 10) setPage(page + 10);
  };
  const antPage = () => {
    if (page > 0) setPage(page - 10);
  };
}
 */
